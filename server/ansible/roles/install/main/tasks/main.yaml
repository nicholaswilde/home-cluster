---
- name: copy K3S service file
  register: k3s_service
  template:
    src: "k3s.service.j2"
    dest: "{{ systemd_dir }}/k3s.service"
    owner: root
    group: root
    mode: 0644

- name: enable and check K3s service
  systemd:
    name: k3s
    daemon_reload: true
    state: restarted
    enabled: true

- name: wait for node-token
  wait_for:
    path: /var/lib/rancher/k3s/server/node-token

- name: register node-token file access mode
  stat:
    path: /var/lib/rancher/k3s/server
  register: p

- name: change file access node-token
  file:
    path: /var/lib/rancher/k3s/server
    mode: "g+rx,o+rx"

- name: read node-token from main
  slurp:
    src: /var/lib/rancher/k3s/server/node-token
  register: node_token

- name: store main node-token
  set_fact:
    token: "{{ node_token.content | b64decode | regex_replace('\n', '') }}"

- name: restore node-token file access
  file:
    path: /var/lib/rancher/k3s/server
    mode: "{{ p.stat.mode }}"

- name: create directory .kube
  file:
    path: ~{{ ansible_user }}/.kube
    state: directory
    owner: "{{ ansible_user }}"
    mode: "u=rwx,g=rx,o="

- name: copy config file to user home directory
  copy:
    src: /etc/rancher/k3s/k3s.yaml
    dest: ~{{ ansible_user }}/.kube/config
    remote_src: true
    owner: "{{ ansible_user }}"
    mode: "u=rw,g=,o="

- name: replace https://localhost:6443 by https://main-ip:6443
  command: >-
    k3s kubectl config set-cluster default
      --server=https://{{ main_ip }}:6443
      --kubeconfig ~{{ ansible_user }}/.kube/config
  changed_when: true

- name: create kubectl symlink
  file:
    src: /usr/local/bin/k3s
    dest: /usr/local/bin/kubectl
    state: link

- name: create crictl symlink
  file:
    src: /usr/local/bin/k3s
    dest: /usr/local/bin/crictl
    state: link

- name: copy config file to local compuater
  become: true
  run_once: true
  ansible.builtin.fetch:
    src: "~{{ ansible_user }}/.kube/config"
    dest: "~/.kube/config-turing-pi"
    flat: true
